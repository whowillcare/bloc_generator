name: bloc_generator
description: A Dart/Flutter code generator for Bloc classes and i18n localization files.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  yaml: ^3.1.2
  args: ^2.4.2
  path: ^1.9.0
  build: ^2.4.1

dev_dependencies:
  build_runner: ^2.4.6

executables:
  generator: generator
